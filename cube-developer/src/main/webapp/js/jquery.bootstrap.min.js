/**
 * message confirm
 */
		$.messager = function() {
			var e = function(e) {
				var c = '<div class="modal fade message-modal-plugin" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel">';
				c += '<div class="modal-dialog modal-sm" style="margin: 15% auto;">';
				c += '<div class="modal-content">';
				c += '<div class="modal-body">';
				c += e;
				c += '</div>';
				c += '<div class="modal-footer">';
				c += '<button type="button" class="btn btn-success" data-dismiss="modal">确认</button>';
				c += '</div></div></div></div>';
				var m = $(".message-modal-plugin");
				if (m.html() != undefined) {
					m.remove();
				}
				$("body").append(c);
				$(".message-modal-plugin").modal('show')
			}, m = function(e, f) {
				var c = '<div class="modal fade message-modal-plugin" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel">';
				c += '<div class="modal-dialog modal-sm" style="margin: 15% auto;">';
				c += '<div class="modal-content">';
				c += '<div class="modal-body">';
				c += e;
				c += '</div>';
				c += '<div class="modal-footer">';
				c += '<button type="button" class="btn btn-success confirm" >确认</button>';
				c += '</div></div></div></div>';
				var m = $(".message-modal-plugin");
				if (m.html() != undefined) {
					m.remove();
				}
				$("body").append(c);
				m = $(".message-modal-plugin");
				$(".message-modal-plugin").modal('show')
				m.find(".confirm").click(function() {
					m.modal('hide');
					f();
				});
			};
			return {
				alert : e,
				confirm : m
			};
		}(jQuery),
		$.confirm = function() {
			var e = function(e, f) {
				var c = '<div class="modal fade confirm-modal-plugin" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel">';
				c += '<div class="modal-dialog modal-sm" style="margin: 15% auto;">';
				c += '<div class="modal-content">';
				c += '<div class="modal-body">';
				c += e;
				c += '</div>';
				c += '<div class="modal-footer">';
				c += '<button type="button" class="btn btn-success confirm" >确认</button>';
				c += '<button type="button" class="btn btn-success" data-dismiss="modal">取消</button>';
				c += '</div></div></div></div>';
				var m = $("body .confirm-modal-plugin");
				if (m.html() != undefined) {
					m.remove();
				}
				$("body").append(c);
				m = $("body .confirm-modal-plugin");
				m.modal('show');
				m.find(".confirm").click(function() {
					f();
					m.modal('hide');
				});
			};
			return e;
		}(jQuery),
		$.fn.menu = function() {
			var e = function(m) {
				var t = $(this);
				var c = '';
				for (var i = 0; i < m.length; i++) {
					if (m[i].sub != undefined && m[i].sub.length > 0) {
						var s = m[i].sub;
						c += '<li class="m-parent">';
						c += '<a><strong><h5>&nbsp;&nbsp;'
								+ m[i].icon
								+ '&nbsp;&nbsp;'
								+ m[i].title
								+ '<span  class="m-state glyphicon glyphicon-chevron-left pull-right" aria-hidden="true">&nbsp;</span></h5></strong></a>';
						c += '<ul>';
						for (var x = 0; x < s.length; x++) {
							c += '<li>';
							c += '<a href="' + s[x].href + '" title="'
									+ s[x].title + '"><strong><h5>&nbsp;&nbsp;'
									+ s[x].icon + '&nbsp;&nbsp;' + s[x].title
									+ '</h5></strong></a>';
							c += '</li>';
						}
						c += '</ul>';
					} else {
						c += '<li>';
						c += '<a href="' + m[i].href + '" title="' + m[i].title
								+ '"><strong><h5>&nbsp;&nbsp;' + m[i].icon
								+ '&nbsp;&nbsp;' + m[i].title
								+ '</h5></strong></a>';
					}
					c += '</li>';
				}
				t.html(c);
				t
						.find(".m-parent a")
						.click(
								function() {
									var tt = $(this);
									if (tt.find('.glyphicon-chevron-left').length != 0) {
										tt
												.find('.m-state')
												.removeClass(
														'glyphicon-chevron-left')
												.addClass(
														'glyphicon-chevron-down');
										tt.next('ul').show(300);
									} else {
										tt
												.find('.m-state')
												.removeClass(
														'glyphicon-chevron-down')
												.addClass(
														'glyphicon-chevron-left');
										tt.next('ul').hide(300);
									}
								})
				t.find("a").click(function() {
					var tt = $(this);
					t.find("a").removeClass('m-active');
					tt.addClass("m-active");
				})
			}
			return e;
		}(jQuery)/*,
		Date.prototype.format = function(format) {
			var date = {
				"M+": this.getMonth() + 1,
				"d+": this.getDate(),
				"h+": this.getHours(),
				"m+": this.getMinutes(),
				"s+": this.getSeconds(),
				"q+": Math.floor((this.getMonth() + 3) / 3),
				"S+": this.getMilliseconds()
			};
			if (/(y+)/i.test(format)) {
				format = format.replace(RegExp.$1, (this.getFullYear() + '').substr(4 - RegExp.$1.length));
			}
			for (var k in date) {
				if (new RegExp("(" + k + ")").test(format)) {
					format = format.replace(RegExp.$1, RegExp.$1.length == 1 ? date[k] : ("00" + date[k]).substr(("" + date[k]).length));
				}
			}
			return format;
		}*/